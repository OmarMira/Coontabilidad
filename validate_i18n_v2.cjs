const fs = require('fs'); const fileES = './src/assets/locales/es.json'; const fileEN = './src/assets/locales/en.json'; function validate(file) { console.log('Validating ' + file + '...'); try { const content = fs.readFileSync(file, 'utf8'); const json = JSON.parse(content); console.log(' JSON Syntax Valid.'); if (json['navigation.dashboard']) { console.log(' Root key navigation.dashboard found'); } else { console.log(' navigation.dashboard NOT found'); } } catch (e) { console.error(' Error: ' + e.message); } } validate(fileES); validate(fileEN);
