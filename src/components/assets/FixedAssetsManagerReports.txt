      {/* Report Modals */}
      {activeReport === 'register' && (
        <div className="fixed inset-0 bg-black/70 backdrop-blur-sm flex items-center justify-center z-50 p-6">
          <div className="bg-slate-900 rounded-2xl max-w-7xl w-full max-h-[90vh] overflow-y-auto">
            <div className="p-6">
              <AssetRegisterReport db={db} onClose={() => setActiveReport(null)} />
            </div>
          </div>
        </div>
      )}

      {activeReport === 'schedule' && (
        <div className="fixed inset-0 bg-black/70 backdrop-blur-sm flex items-center justify-center z-50 p-6">
          <div className="bg-slate-900 rounded-2xl max-w-7xl w-full max-h-[90vh] overflow-y-auto">
            <div className="p-6">
              <DepreciationScheduleReport db={db} onClose={() => setActiveReport(null)} />
            </div>
          </div>
        </div>
      )}

      {activeReport === 'disposals' && (
        <div className="fixed inset-0 bg-black/70 backdrop-blur-sm flex items-center justify-center z-50 p-6">
          <div className="bg-slate-900 rounded-2xl max-w-7xl w-full max-h-[90vh] overflow-y-auto">
            <div className="p-6">
              <DisposalSummaryReport db={db} onClose={() => setActiveReport(null)} />
            </div>
          </div>
        </div>
      )}
